8/15/26, 12:53 AM                                                                         Problems - Codeforces


                                              ICPC 2026 Online Challenge 1 powered by Huawei


             The problem statement has recently been changed. View the               ×
                                     changes.


                A. Edge–Cloud Collaborative Scheduling
                                 15 seconds, 256 megabytes

        This is an interactive scheduling problem. The "interactor" sends events         Guide: blue is local, green is the shared two-way link, and orange is
        as they happen; after each event group, you choose ready tasks to start.         remote; computation and transfer may overlap. Figure labels use the
        You never predict future requests. For a fixed test it is deterministic and      glossary above.
        non-adaptive: the same legal responses produce the same events and
                                                                                         What You Control
        score.
                                                                                         You choose:
        IMPORTANT: YES, THERE WILL BE A SYSTEM TEST AFTER THE
        CHALLENGE.                                                                           which legal task to start on each free computer;

        No AI Background Is Needed                                                           the remote computer assigned to a request when scheduling its P
                                                                                             PRE;
        Treat this as jobs moving through computers; no text-generation
                                                                                             how to split an input-stage P PROC into ranges of numbered parts;
        knowledge is needed. A token is one output unit. Commands P PRE/P
                                                                                             and
        PROC/P POST prepare a request once; D PRE/D PROC/D POST
                                                                                             which ready requests to combine into each output group.
        produce one token. Both follow local → remote → local.
                                                                                         Transfers are automatic. For a first solution, use one full input-stage piece
         Term                    Meaning                                                 and groups of size 1.

         edge / local            the one computer beside users                           System Model

         cloud / remote          one of 𝐾 worker computers                                   There is one local computer and 𝐾 identical remote computers,

         token                   one output unit                                             numbered 0 through 𝐾 − 1 and written C0, C1, … . They work
                                                                                             independently and may run while data moves.
         prefill / input stage   one-time input preparation                                  There are 𝑅 requests in total, but 𝑅 is not announced. Request ids
                                                                                             are 𝑖 = 0, 1, … , 𝑅 − 1 in arrival order. The arrival time of request 𝑖 is
         decode / output
                                 repeated work producing one token                           the timestamp of its ARR event. That event reveals its input length
         step
                                                                                             𝐿 in [𝑖]; its output length 𝐿 out [𝑖] remains hidden until a FIN event
         uplink / UP             local-to-remote transfer                                    reports that it has finished.
                                                                                             Request 𝑖 performs one token-free input stage, then exactly 𝐿 out [𝑖]
         downlink / DOWN         remote-to-local transfer
                                                                                             output steps, each producing one token. Thus total generated
         FIFO queue              first transfer queued is the first transfer completed       tokens are ∑ 𝑖 𝐿 out [𝑖] , with 𝐿 out [𝑖] ≥ 1 .
                                                                                             You assign each request to one remote computer in its P PRE task.
         batch / group           requests combined in one output task
                                                                                             The choice is fixed: all later input stage tasks for that request name
         chunk / piece           a consecutive range of input-stage parts                    the same remote computer, and every D PROC containing it runs
                                                                                             there. Output-step tasks on the local computer may combine
                                 transferred request data; its size is defined by the
         activations                                                                         requests from different remote computers.
                                 protocol
                                                                                             Each computer executes at most one task at a time. Once a task
         model layers /                                                                      starts, it cannot be paused. Separately scheduled input-stage pieces
                                 the numbered input-stage range [0, num_layers)
         parts                                                                               may be alternated with other work.
                                                                                             Schedule cost 𝑆 : a task assigned at time 𝑡 with execution duration
        The Problem in One Minute                                                            dur occupies its computer over [𝑡, 𝑡 + 𝑆 + dur]. The cost is paid
                                                                                             once per task, including once per input-stage piece and once per
        One local computer and several remote computers handle requests.
        Each request first prepares its input by making this trip:                           output group. Transfers do not pay 𝑆 .
                                                                                             Execution times are fixed: task duration depends on its step and
                 local computer ⟶ remote computer ⟶ local computer.                          group size (and fraction of parts for input-stage pieces), never on the
                                                                                             output-token position or saved internal data size. See Input — Task-
        The same trip is then repeated once per token. The first trip is the input
                                                                                             Time Table. Finished tasks and transfers are reported as TDN and
        stage; each later trip is an output step. Transfers are automatic; you
        choose ready tasks for free computers.                                               XDN.

        The score rewards output rate and short waits. Time to decode ready              Request Lifecycle
        (TDR) ends when the first output step can begin, before a token is
                                                                                         Solid arrows are scheduling dependencies; dashed arrows are automatic
        produced. Time per output token (TPOT) is the mean gap between
                                                                                         transfers.
        consecutive tokens.

        Challenge Format

        Two worked examples appear at the end of the statement. Test 1 is the
        public worked Example 1, tests 2–22 are hidden preliminary tests, and
        finals use a separate frozen set. Per-test scores are reported on the 0–
        1000 scale defined below, and the contest system aggregates them
        according to the contest rules; no hacks are used.




https://codeforces.com/contest/2251/problems#                                                                                                                             1/8
8/15/26, 12:53 AM                                                                       Problems - Codeforces
                                                                                           come from different D PRE groups and different local-to-remote
                                                                                           transfers. Completion queues one remote-to-local transfer (len = this
                                                                                           group's size).
                                                                                           D POST (local computer, across remote computers). May start after,
                                                                                           for each request: the remote-to-local transfer XDN carrying that
                                                                                           member's current-iteration result (different D PROC groups/remote-
                                                                                           to-local transfers are fine). Each member's completion produces one
                                                                                           token.




        Guide: TDR ends at P POST; each later D POST makes one token.

        PRE/POST use the local computer; PROC uses the assigned remote;
        transfers use the shared link.

        Input Stage

        Three steps: first (local) → process (assigned remote) → final (local). An
        input-stage group has one request.
                                                                                       Guide: the upper lane splits one input stage into consecutive ranges; the
            P PRE (local computer). May start after: the request's ARR event.          lower lane groups ready requests for output.
            Fixes the remote computer; on completion, the local-to-remote              Groups: 𝑚 ≥ 1 , request ids distinct, every member satisfying its
            transfer is queued immediately (whether or not that remote computer        predecessor rule; 𝑚 = 1 is always allowed. Only output work is ever
            is busy).                                                                  grouped — input-stage groups always contain exactly one request.
            P PROC (remote computer) computes all num_layers numbered                  There is no other group-size limit: no maximum exists or is announced
            parts. The simplest choice is one full piece [0, num_layers). You          — any nonempty set of distinct, currently ready requests may be
            may split it so other remote work can run between pieces.                  grouped, subject only to the step and remote computer rules above.
            P POST (local computer). May start after: the request's input stage        Order of request ids within a group: see Output.
            remote-to-local transfer XDN. Completion makes the request ready           Input
            for producing output; TDR is measured from arrival to this                 Protocol Guide
            completion.
                                                                                       P is the one-time input stage; D is one output step. PRE/POST are local
        Exact splitting rules. Each piece is a nonempty integer range [𝑙𝑠, 𝑙𝑒): it     tasks and PROC is remote. Events are ARR (arrival), TDN (task done),
        includes part 𝑙𝑠 and stops before part 𝑙𝑒 . For each request, pieces must      XDN (transfer done), and FIN (finished). In [𝑙𝑠, 𝑙𝑒), 𝑙𝑠 is included and 𝑙𝑒
        be issued in ascending, gap-free order: the first starts at 0, each later 𝑙𝑠   excluded. Input arrives interactively: read each whole frame and respond
        equals the previous 𝑙𝑒 , and the last ends at num_layers. Thus there are       once. Times are real milliseconds; counts, lengths, and ids are integers;
        at most num_layers pieces, and none when num_layers = 1 beyond                 fields use single spaces.
        the full piece. The first piece waits for the input stage local-to-remote
                                                                                       Startup Configuration
        transfer XDN; each later piece waits for the previous piece's TDN. Its
        duration is                                                                    The interactor first sends two lines (no response expected): the system
                                                                                       parameters, then the scoring parameters:
                             𝑙𝑒 − 𝑙𝑠
                                      × prefill_proc(𝐿 in [𝑖]).
                           num_layers
        Only the last piece queues the input stage remote-to-local transfer, of        K S latency_in_ms bandwidth_gbps bytes_per_token
        length 𝐿 in [𝑖] . No transfer occurs between pieces. Any range that is         num_layers
        empty, outside the part interval, out of order, or leaves a gap is a           SLO1 SLO2 tp_UB tp_base dist_base w_tp w_c
        violation.                                                                     𝐾 , bytes_per_token, and num_layers are integers; all other values are
                                                                                       reals (times in ms, output rates in tokens/ms). SLO stands for service-
        Output Steps
                                                                                       level objective: SLO1 is the TDR target and SLO2 is the TPOT target. The
        After the input stage, request 𝑖 performs 𝐿 out [𝑖] output steps in            other names on the scoring line mean: tp_UB = 𝑡𝑝UB , tp_base = 𝑡𝑝base ,
        succession: completing iteration 𝑘's final step readies iteration 𝑘 + 1. The   dist_base = 𝑑𝑖𝑠𝑡base , w_tp = 𝑤 tp , w_c = 𝑤 𝑐 . bandwidth_gbps is in
        same three steps are used, but multiple requests may be grouped per            gigabits per second (Gb/s). The value bytes_per_token is already the
        step. You choose each group independently. Output-step tasks cannot            complete data size for one token; do not multiply it by an element width.
        be split into part ranges.
                                                                                       Communication
            D PRE (local computer, across remote computers): may group
                                                                                       Transfers are automatic: the interactor queues them when their triggering
            requests assigned to any mix of remote computers — the listed
                                                                                       computation finishes; you never output a transfer command. A single
            decode_pre time depends only on total group size, so across remote         link connects the local computer and remote computer side and is
            computers grouping improves local computer efficiency (the task-
                                                                                       shared by all remote computers. UP means a local-to-remote transfer
            time table plus the transfer formula is the only efficiency model;         (local computer → remote computer), and DOWN means a remote-to-
            nothing else rewards grouping). May start after, for each request:         local transfer (remote computer → local computer). These are
            completion of that request's previous final step (P POST for its first     independent one-at-a-time FIFO queues: transfers finish in the order they
            output step, otherwise its previous D POST) — and the request must         enter, and both directions may be active simultaneously. For
            not be finished (FIN always arrives with the final D POST TDN, so          simultaneous queues, one D PRE's per-remote transfers enter by
            you always know). Completion queues one local-to-remote transfer           increasing remote index. Otherwise transfers enter in interactor event
            per distinct remote computer in the group (len = that remote               order; tasks started together and finishing together use assignment-line
            computer's member count), enqueued simultaneously in increasing            order.
            remote computer index order.
            D PROC (remote computer): all members must be assigned to that
            remote computer. May start after, for each request: the local-to-
            remote transfer XDN carrying that member's current-iteration data —
            the rule is checked separately for each request: members may


https://codeforces.com/contest/2251/problems#                                                                                                                        2/8
8/15/26, 12:53 AM                                                                           Problems - Codeforces
        Transfer time                                                                          ARR <rid> <𝐿 in [𝑖] > — request 𝑖 = 𝐫𝐢𝐝 arrived; its arrival time is this
        = latency_in_ms + 8 data_bytes/(bandwidth_gbps × 106 ) ms, where                       frame's timestamp 𝑡 ; 𝐿 in [𝑖] is given, while 𝐿 out [𝑖] is unknown.
        data_bytes = len × bytes_per_token . The factor 8 converts bytes to                    TDN <server> <task_spec> <dur> — a task completed and its
        bits. For an input-stage transfer of request 𝑖 , len = 𝐿 in [𝑖]; for output, use       server is now free. task_spec is echoed in a canonical equivalent
        the per-remote computer local-to-remote transfer and per-group remote-
                                                                                               form: integers use ordinary decimal notation and fields use single
        to-local transfer sizes defined in Legend. Guaranteed
                                                                                               spaces; dur is the execution duration, excluding 𝑆 , printed with 9
        latency_in_ms > 0 : every transfer takes strictly positive time.
                                                                                               digits after the decimal point.
        Task-Time Table                                                                        XDN <UP|DOWN> <remote> <size> <PRE|DEC> <m> <rid...> — a
        Before requests arrive, the judge gives you a table telling you how long               transfer completed (its data has arrived). size is in bytes (
        tasks take, in milliseconds. You only read this table; you do not measure              = len × bytes_per_token); PRE/DEC marks an input-stage or
        the times yourself. Read an integer 𝑁 , then 𝑁 rows of 7 values; no                    output-step transfer; per-remote computer transfers each produce
        response is expected:                                                                  their own XDN; for input stage, m = 1 . The m request ids listed are
                                                                                               exactly the requests whose data the transfer carries: the single
                                                                                               request for input stage; that remote computer's members of the
        batch_size       prefill_pre        prefill_proc         prefill_post                  triggering D PRE for an output-step local-to-remote transfer; the
        decode_pre       decode_proc        decode_post                                        members of the triggering D PROC group for an output-step remote-
        Here batch_size means the number of requests grouped into one task.                    to-local transfer.
        The names containing prefill refer to the input stage, and the names                   FIN <rid> — the request finished all output steps (its last token was
        containing decode refer to output steps. These fixed names come from                   just produced). It must not appear in any task you assign from now
        the protocol; no AI knowledge is required. The values help you compare
                                                                                               on.
        scheduling choices. You must read all rows, but a first correct scheduler
        does not need to use the values at all; a more advanced scheduler will             <server> = E (local computer) or Ck (remote computer 𝑘); <m> is the
        use the complete task-time table, including values between listed rows.            number of request ids that follow. request ids are integers
                                                                                           0, 1, … , 𝑅 − 1, assigned in arrival order, never reused.
        batch_size means 𝐿 in for the three input stage columns (one request per
        input stage group) and member count for the three output columns. The              Constraints
        batch_size values are distinct positive integers in [1, 4096]. A listed
        output-step group size may exceed the test's 𝑅; such a row only defines                1 ≤ 𝐾 ≤ 8 ; 1 ≤ 𝑅 ≤ 2000 ; 1 ≤ 𝐿 in [𝑖] ≤ 4096 ; 1 ≤ 𝐿 out [𝑖] ≤ 512 ;
        the task-time table and does not make that group size schedulable. Not                 ∑ 𝑖 𝐿 out [𝑖] ≤ 2 ⋅ 105 per test; 1 ≤ num_layers ≤ 64;
        every step is listed at every group size; a missing value is -1. Rows are              2 ≤ 𝑁 ≤ 4096 .
        given in no guaranteed order. Guarantee: every step column contains at                 1 ≤ 𝑆 ≤ 10; 0.001 ≤ latency_in_ms ≤ 50;
        least one non-missing entry.                                                           0.001 ≤ bandwidth_gbps ≤ 100 ; 1 ≤ bytes_per_token ≤ 106 .
        All listed values are execution durations excluding the schedule cost                  0.001 ≤ SLO1, SLO2 ≤ 109 ; 0 ≤ 𝑡𝑝base , 𝑑𝑖𝑠𝑡base ≤ 109 ;
        𝑆 , exactly like the dur field of every TDN: the total time for which a                10−9 ≤ 𝑡𝑝UB ≤ 109 ; and 𝑡𝑝UB > 𝑡𝑝base .
        computer is busy with a task is always 𝑆 + dur (see System Model). For                 Every non-missing task-table entry is in [0.001, 104 ] ms. System
        each step, sort the available rows by group size. If the needed size is                reals, task times, timestamps, and durations use 9 decimal places.
        listed, use its time. If it lies between two listed sizes, draw a straight line        Arrival timestamps are nondecreasing in [0, 109 ] ms. Completion
        between their times and use the value on that line. Below the smallest                                           9
                                                                                               frames may exceed 10 , but the validator guarantees a conservative
        size, use the first time; above the largest size, use the last time. The result                             12
                                                                                               upper bound of 10 ms even if every legal task and transfer is
        is the same on every remote computer and never changes. Guarantee:                     serialized; all timestamps are nonnegative finite doubles.
        every resulting legal task duration is strictly positive. Here 𝑅 is the test's
                                                                                               You never need to predict event timestamps to keep the protocol
        total request count (see Constraints); 𝑅 is not announced during the
                                                                                               correct: every completion is announced by the interactor (TDN/XDN)
        interaction and is not a separate group-size limit. No maximum output
                                                                                               with its timestamp and duration, so a purely reactive scheduler needs
        group size exists beyond the number of currently ready requests. The
                                                                                               no arithmetic on future times. If you simulate ahead for planning, use
        duration of every completed task is echoed in its TDN's dur field.
                                                                                               ordinary double-precision arithmetic with the piecewise-linear lookup
        Event Frames (Your Turns)                                                              and transfer formula above.
                                                                                                               6
        The interactor sends a frame whenever one or more events occur. A                      At most 2 ⋅ 10 frames per test; use fast I/O.
        frame contains: one timestamp line t, one event-count line e, then 𝑒 event             Degenerate values (e.g. 𝐾 = 1, num_layers = 1) occur and simply
        lines. Always read the whole frame before deciding what to do.                         disable the corresponding mechanic.

        The frame timestamp is its event time, printed with 9 decimal places;              Output
        printed timestamps are nondecreasing, while the internal event times of            After every frame (one turn), print a count 𝑛 (0 ≤ 𝑛 ≤ 𝐾 + 1 ), followed by
        consecutive frames are strictly increasing. Only events with exactly the           𝑛 assignments. For readability the examples put one assignment on each
        same internal timestamp are coalesced. A later event is never moved to             line, but the parser accepts arbitrary whitespace between tokens. Each
        an earlier frame, and line order carries no scheduling priority. Your              assignment has the form <server> <task_spec>. It starts one task on a
        response may use any event in the frame: every resource freed by a TDN             computer that is currently free. The command words shown below are
        is free, even if that event is the last line. Guarantee: FIN appears beside        fixed output syntax; copy them exactly. Integer tokens use the ordinary
        the TDN of that request's final output final step. After reading such a            signed-decimal syntax accepted by C++ conversion (an optional leading
        frame, the request is finished and must not appear in your response to             + and leading zeros are accepted); echoed integers are canonical
        that frame or any later response.                                                  decimal. Thus a response containing no assignments is simply



                                                                                           0
                                                                                           For a first scheduler, most responses may contain only zero or one task.
                                                                                           Unmentioned resources stay idle. You may leave any free computer idle
                                                                                           even when one or more legal tasks are available. Waiting can be a useful
                                                                                           grouping choice, but do not create the stuck state with no future event
                                                                                           described in Interaction. Every frame is a scheduling opportunity,
                                                                                           including frames containing only transfers or arrivals. After reading the
                                                                                           whole frame, you may use any predecessor or free resource reported
                                                                                           anywhere in it.
        Guide: read the whole event frame, update state, then print one count
        and exactly that many assignments.


https://codeforces.com/contest/2251/problems#                                                                                                                              3/8
8/15/26, 12:53 AM                                                                     Problems - Codeforces
        When deciding whether a task is legal, ask three questions: Is its               D POST — Server: local computer. Requires: the remote-to-local
        computer free? Has every required predecessor event arrived? Is every            XDN carrying each member's current-iteration result. Completes:
        request in the task at exactly this step and not already in flight or            produces one token per member; after iteration 𝐿 out [𝑖], produces
        finished? The tables below make these checks precise.
                                                                                         FIN.
        All assignments in one response begin simultaneously at timestamp 𝑡 .
        One cannot depend on another assignment from that same response.             Interaction
                                                                                     Each test is a separate run of your program. Flush the output stream
        Assign at most one task to each resource; it becomes free again only
        when its TDN arrives. The six legal task shapes are:                         after every response. In C++, use cout « flush; after printing the
                                                                                     complete response; we recommend ios::sync_with_stdio(false);
                                                                                     cin.tie(nullptr); for fast input. In Python, call sys.stdout.flush() after
         step             task_spec                 Server
                                                                                     printing the complete response (or use print(..., flush=True) for its final
         input stage      P PRE <remote>            local computer (also fixes the   line).
         first step       <rid>                     remote computer)
                                                                                     The interaction proceeds: startup configuration (2 lines) → Task-Time
                                                                                     Table (𝑁 + 1 lines) → repeat {frame → your response} → END. Your
         input stage      P PROC <ls> <le>
                                                    that remote computer             first response follows the first frame. The whole program follows this
         process          <remote> <rid>
                                                                                     loop:
         input stage      P POST <remote>
                                                    local computer
         final step       <rid>
                                                                                     read the 2 parameter lines, then N and the N warmup
         output first                               local computer (across           rows
                          D PRE -1 <m> <rid...>
         step                                       remote computers)                loop:
                                                                                         read one line; if it is END: exit
         output           D PROC <remote>
                                                    that remote computer                    parse it as timestamp t; read event count e from
         process          <m> <rid...>
                                                                                     the next line
         output final     D POST -1 <m>             local computer (across                  read the e event lines
         step             <rid...>                  remote computers)                    update your state (completions, arrivals,
                                                                                     transfers, FINs)
        For D PRE/D POST, the -1 marks a group spanning remote computers;                   choose assignments for currently free resources
        for D PROC, all members must be assigned to <remote>. In P PRE,              (possibly none)
        <remote> must be in [0, 𝐾 ) ; in P PROC and P POST, <remote> must                 print n and the n assignment lines; flush
        equal the request's assigned remote computer — anything else is a            For a simple implementation, store one state per request. Its path is
        violation. (P POST itself runs on the local computer: its <remote> field
        carries no scheduling meaning and is purely a consistency check against
        the request's fixed assignment.) Iteration indices are never transmitted:    ARR -> P PRE -> input stage UP -> P PROC piece(s) ->
        each request's steps are strictly sequential, so your own bookkeeping is     input stage DOWN -> P POST
        the sole — and sufficient — source of truth for which iteration a task              -> D PRE -> output UP -> D PROC -> output DOWN ->
        belongs to.                                                                  D POST
        The order of request ids within a group has no semantic effect: legality,          -> either FIN or the next D PRE
        durations, transfer sizes, and all subsequent events depend only on the      Move a request to its next state only when the corresponding event
        member set (D PRE -1 3 4 7 9 and D PRE -1 3 9 4 7 denote the same            appears in a frame. Mark a computer busy when you assign it and free
        task). Order is preserved on echo: TDN repeats the same fields in            only when its TDN appears. Tracking these states is enough for a correct
        canonical decimal form with single spaces, and every XDN lists its           solution. You do not need to predict when tasks will finish.
        request ids in the order of the triggering task's specification — a D PRE    You act only when a frame arrives. There is no timer or self-wake
        group spanning remote computers has per-remote computer local-to-            mechanism: you cannot ask to be woken at a chosen future time, and
        remote transfer lists that remote computer's members as a subsequence        you cannot deliberately idle a computer until an arbitrary moment. A task
        of your D PRE line, an output-step remote-to-local transfer lists the        can only be assigned in response to a frame, at that frame's timestamp. If
        triggering D PROC group in your order.                                       you delay an action, you must wait for a future event frame.

        Dependency Summary                                                           Mistakes That Give Zero Points on a Test
        Each task is listed separately so its predecessor and completion effect      The following errors score 0 on the test. A legal but slow choice is still
        are easy to scan. For a group, the predecessor rule applies to every         valid; it only lowers your score.
        member.
                                                                                         Assigning a task to a busy computer, or two tasks to one resource in
            P PRE — Server: local computer. Requires: the request's ARR                  a single response.
            event. Completes: fixes the request's remote computer and queues             Assigning a task before all of its required earlier events have been
            its input-stage local-to-remote transfer.                                    delivered. This includes referencing a rid that has not arrived and re-
            P PROC piece — Server: the assigned remote computer. Requires:               issuing a completed step.
            for the first piece, the input-stage local-to-remote XDN; for a later        Including a request that is already part of an in-flight task, or that has
            piece, the previous piece's TDN. Completes: only the last piece (            already finished (FIN).
            𝑙𝑒 = num_layers) queues the input-stage remote-to-local transfer.            Wrong remote computer: P PROC/P POST with a remote computer
            P POST — Server: local computer. Requires: the request's input-              other than the request's assigned remote computer; P PRE with a
            stage remote-to-local XDN. Completes: stops TDR and makes the                remote computer outside [0, 𝐾 ); a D PROC member assigned to a
            request ready for output.                                                    different remote computer.
            D PRE — Server: local computer. Requires: each member's                      Illegal piece: empty (𝑙𝑠 = 𝑙𝑒 ), outside [0, num_layers], or not
            previous final-step TDN (P POST or its previous D POST), and the             ascending and gap-free.
            request must not be finished. Completes: queues one local-to-                Malformed group: 𝑚 < 1 or duplicate request ids.
            remote transfer per remote computer represented in the group, in             Malformed or unparsable output.
            increasing remote-computer index.                                            Reaching a stuck state with unfinished requests and no possible
            D PROC — Server: the assigned remote computer. Requires: the                 future event (see below).
            local-to-remote XDN carrying each member's current-iteration data.           Exceeding the time or memory limit.
            Completes: queues one remote-to-local transfer whose length is the
                                                                                     Errors and stream closure
            group size.

https://codeforces.com/contest/2251/problems#                                                                                                                         4/8
8/15/26, 12:53 AM                                                                        Problems - Codeforces
        The interactor never produces -1 as a failure response. This is unrelated       waiting-time component = 𝖼𝗅𝖺𝗆𝗉(dist; 𝑑𝑖𝑠𝑡base , 0) — the same
        to the mandatory -1 marker in participant commands D PRE and D                  conversion with lower values treated as better: lower dist is better, 1 at
        POST. On a protocol violation or malformed response it simply stops: the        dist = 0, 0 at the reference scheduler's amount above the waiting-time
        test scores 0 and no further frames are sent. If reading input ever fails or    targets 𝑑𝑖𝑠𝑡base . Written out directly (the form easiest to implement and
        reaches end-of-file, exit immediately with exit code 0 — do not block           verify):
        waiting for more input and do not crash; the verdict is determined by the
                                                                                                                 ⎧ max 0, 1 − dist/𝑑𝑖𝑠𝑡                     if 𝑑𝑖𝑠𝑡base > 0,
        interactor, not by your exit path.                                                                       ⎪
                                                                                                                      (                base )
                                                                                        waiting-time component = ⎨ 1
                                                                                                                 ⎪
                                                                                                                                                            if 𝑑𝑖𝑠𝑡base = 0 an
        Getting Stuck                                                                                            ⎩0                                         if 𝑑𝑖𝑠𝑡base = 0 an
        If unfinished requests remain but no task, transfer, or future arrival can
                                                                                        Important special case: if 𝑑𝑖𝑠𝑡base = 0, the waiting-time component is
        create another event, the run is stuck. The interactor detects this stuck
                                                                                        either 0 or 1. It is 1 only when both mean-waiting-time targets are met (
        state, terminates, and assigns 0 to the test. While future arrivals remain,
                                                                                        dist = 0), and 0 otherwise.
        responding with 0 assignments is safe because time advances to the
        next arrival. Delaying a request hurts the score; permanently abandoning
        it can cause this stuck state.

        Termination

        When all requests have finished, the interactor sends END (a single line,
        in place of the next frame's header) after reading your response to the
        final frame. Read it and exit.

        The total number of requests 𝑅 is not announced in advance and there is
        no "no more arrivals" signal — you cannot distinguish a lull from the end
        of the stream. This is intentional; plan your grouping accordingly.
                                                                                        Diagram guide: the upper branch rewards higher output rate; the lower
        Scoring                                                                         branch rewards smaller normalized TDR/TPOT target violations. The two
        You do not need to calculate the score to write a valid scheduler. First        [0, 1] components are weighted, added, and multiplied by 1000 .
        make a scheduler that finishes every request legally. Read the formula
        only when you are ready to improve its score. It balances two intuitive
                                                                                        𝖭𝗈𝗋𝗆𝖺𝗅𝗂𝗓𝖾𝖽𝖲𝖼𝗈𝗋𝖾 = 𝑤 tp ⋅ 𝖼𝗅𝖺𝗆𝗉(tp; 𝑡𝑝base , 𝑡𝑝UB ) + 𝑤 𝑐 ⋅ 𝖼𝗅𝖺𝗆𝗉(dist; 𝑑𝑖𝑠𝑡bas
        goals:
                                                                                        𝖲𝖼𝗈𝗋𝖾 = 1000 ⋅ 𝖭𝗈𝗋𝗆𝖺𝗅𝗂𝗓𝖾𝖽𝖲𝖼𝗈𝗋𝖾.
                                                                                        Thus each completed test awards a score in [0, 1000]. Verdicts. A
            Output rate: finish more output tokens per millisecond of simulated
                                                                                        completed interaction scores as above. A protocol error, malformed
            time.
                                                                                        output, stuck state with no future event, or exceeding the time/memory
            waiting time: make requests ready for output promptly and avoid             limit scores 0 on that test; other tests are unaffected. No partial credit is
            long gaps between produced tokens.                                          awarded for an unfinished test.

        Each goal becomes a component in [0, 1]. The weights 𝑤 tp and 𝑤 𝑐 say           Contest aggregation. The 22 preliminary tests provide feedback and do
        how much that test values each goal, and the final score is their weighted      not contribute to the final ranking. The final score is the arithmetic mean
        sum times 1000 .                                                                of the 20 frozen final-test scores. Ranking uses that mean before display
                                                                                        rounding; the displayed score is rounded to three digits after the decimal
        The exact guarantees are: 𝑤 tp , 𝑤 𝑐 ≥ 0; 𝑤 tp + 𝑤 𝑐 = 1 ; SLO1 > 0;
                                                                                        point. Therefore a known per-test vector of 0, 500, and 1000 -point
        SLO2 > 0; 𝑡𝑝UB > 𝑡𝑝base ; 𝑑𝑖𝑠𝑡base ≥ 0 . Either weight may be 0, but
                                                                                        outcomes contributes exactly those values before averaging. This
        every request must still be completed and every input/output rule
                                                                                        problem is not open to hacks.
        obeyed. The following formula turns a value into a number from 0 to 1:

        𝖼𝗅𝖺𝗆𝗉(𝑥; base, target) = max(0, min(1, (𝑥 − base)/(target − base)))
        (0 at base, 1 at target, clamped outside).

        Output-Rate Component. tp = ∑ 𝑖 𝐿 out [𝑖] / total elapsed time
        tokens/ms, where total elapsed time = (the latest final-token production
        time over all requests) − (the earliest request arrival time), in ms. Output
        rate component = 𝖼𝗅𝖺𝗆𝗉(tp; 𝑡𝑝base , 𝑡𝑝UB ) . The value 𝑡𝑝base comes
        from a fixed one-request-at-a-time reference schedule; 𝑡𝑝UB is an
        estimated high rate. At or below the baseline the component is 0; at or
        above the upper bound it is 1; between them it grows linearly. These
        values set scoring and are not promises about any solution in the testing
        kit.

        Waiting-Time Component. The input gives two waiting-time targets.
        SLO1 is the target average wait until a request is ready for its first output
        step (time to decode ready, TDR). No token has been produced at this
        point. SLO2 is the target mean gap between consecutive produced
        tokens (TPOT). The letters SLO are only part of the fixed input names. For
        request 𝑖 , let 𝑒1 < 𝑒2 < ⋯ < 𝑒𝐿 out [𝑖] be its token production times, i.e.
        the completion times of its output D POST tasks.

            tdr = mean over all requests of (input stage final step completion −
            arrival).
            tpot = mean of 𝑒𝑗+1 − 𝑒𝑗 over all consecutive output gaps of all
            requests pooled (1 ≤ 𝑗 < 𝐿 out [𝑖]). A request with 𝐿 out [𝑖] = 1
            contributes no gaps. If no request contributes a gap, tpot is defined
            as 0.

                                  tdr − SLO1                                    tpot − SLO2
        excess_tdr = max(0,                  ,         excess_tpot = max(0,                   ,
                                     SLO1 )                                        SLO2     )




                          dist = √‾excess_tdr
                                    ‾‾‾ ‾‾‾ ‾‾‾‾2 ‾‾‾ ‾‾‾‾ ‾‾‾ ‾‾‾ 2‾ .
                                                  + excess_tpot


https://codeforces.com/contest/2251/problems#                                                                                                                              5/8
8/15/26, 12:53 AM                                                                     Problems - Codeforces
         input                                                                        0.000000000                               1
         1 1.000000000 2.000000000 1.000000000 125000 4
         30.000000000 15.000000000 0.062500000 0.022222222                            1                                         E P PRE 0 0
         0.000000000 0.500000000 0.500000000
         2                                                                            ARR 0 4
         1 3.000000000 10.000000000 2.000000000 1.000000000
         4.000000000 1.000000000
         4 3.000000000 10.000000000 2.000000000 1.000000000                           4.000000000                               0
         4.000000000 1.000000000
         0.000000000
                                                                                      1
         1
         ARR 0 4
                                                                                      TDN E P PRE 0 0 3.000000000
         4.000000000
         1
         TDN E P PRE 0 0 3.000000000
         10.000000000                                                                 10.000000000                              1
         1
         XDN UP 0 500000 PRE 1 0                                                      1                                         C0 P PROC 0 4 0 0
         21.000000000
         1                                                                            XDN UP 0 500000 PRE 1 0
         TDN C0 P PROC 0 4 0 0 10.000000000
         27.000000000
         1
         XDN DOWN 0 500000 PRE 1 0                                                    21.000000000                              0
         30.000000000
         1                                                                            1
         TDN E P POST 0 0 2.000000000
         32.000000000                                                                 TDN C0 P PROC 0 4 0 0 10.000000000
         1
         TDN E D PRE -1 1 0 1.000000000
         35.000000000
         1                                                                            27.000000000                              1
         XDN UP 0 125000 DEC 1 0
         40.000000000                                                                 1                                         E P POST 0 0
         1
         TDN C0 D PROC 0 1 0 4.000000000                                              XDN DOWN 0 500000 PRE 1 0
         43.000000000
         1
         XDN DOWN 0 125000 DEC 1 0
         45.000000000
                                                                                      30.000000000                              1
         2
         TDN E D POST -1 1 0 1.000000000                                              1                                         E D PRE -1 1 0
         FIN 0
         END                                                                          TDN E P POST 0 0 2.000000000

         output
         1                                                                            32.000000000                              0
         E P PRE 0 0
         0                                                                            1
         1
         C0 P PROC 0 4 0 0                                                            TDN E D PRE -1 1 0 1.000000000
         0
         1
         E P POST 0 0
         1                                                                            35.000000000                              1
         E D PRE -1 1 0
         0                                                                            1                                         C0 D PROC 0 1 0
         1
         C0 D PROC 0 1 0                                                              XDN UP 0 125000 DEC 1 0
         0
         1
         E D POST -1 1 0
         0                                                                            40.000000000                              0

                                                                                      1
        Examples

        If interactive problems are new to you, begin with Example 1 and focus        TDN C0 D PROC 0 1 0 4.000000000
        on the repeating pattern: read a complete frame on the left, update your
        state, then print one complete response on the right. The exact
                                                                                      43.000000000                              1
        timestamps are consequences of the supplied costs; your scheduler
        never has to predict them. The transcript below shows the participant-        1                                         E D POST -1 1 0
        side input and output directly; output responses contain no timestamps.
                                                                                      XDN DOWN 0 125000 DEC 1 0
        The examples use 𝑆 =1 , latency_in_ms=2 , bandwidth_gbps=1, and
        bytes_per_token=125000 . Because bandwidth is in gigabits per
        second, a one-token transfer takes 2 + 8 ⋅ 125000/106 = 3 ms. Every           45.000000000                              0
        table has the interactor's input on the left and your complete response on
        the right. Each protocol line occupies its own table row. A response          2
        begins with its assignment count.
                                                                                      TDN E D POST -1 1 0 1.000000000
        Example 1: one request, end to end.
                                                                                      FIN 0
        Here 𝐾 =1, num_layers=4, request 0 has 𝐿 in [0] = 4 and 𝐿 out [0] = 1 ,
        and the relevant execution durations are 3, 10, 2 ms for input stage and
        1, 4, 1 ms for output. The scheduler uses one full input-stage piece.         END                                       exit


         Interactor                                Your response                     The input-stage final step completes at 30.000 , so this request's TDR is
                                                                                     30 ms. Its only token is produced at 45.000 . Because 𝐿 out [0] = 1 , it
                                                                                     contributes no TPOT gap.


https://codeforces.com/contest/2251/problems#                                                                                                                    6/8
8/15/26, 12:53 AM                                                                        Problems - Codeforces
        Example 2: grouping requests from two remote computers.                         An output task's time depends only on its group size, never on which
                                                                                        output token is being generated or how many tokens are in the request's
        Assume requests 0 and 1 have already been assigned to C0 and C1,                previously stored data. The task-time table and transfer formula are the
        respectively, and both are ready for their first output step at time 16.500 .   complete cost model.
        This excerpt shows one across-remote-computers first step group, two
        single-remote-computer process tasks, and one across-remote-
        computers final step group.


         Interactor                                Your response



         16.500000000                              1

         1                                         E D PRE -1 2 0 1

         TDN E P POST 1 1 1.250000000


         19.000000000                              0

         1

         TDN E D PRE -1 2 0 1 1.500000000



         22.000000000                              1

         1                                         C0 D PROC 0 1 0

         XDN UP 0 125000 DEC 1 0



         25.000000000                              1

         1                                         C1 D PROC 1 1 1

         XDN UP 1 125000 DEC 1 1



         27.000000000                              0

         1

         TDN C0 D PROC 0 1 0 4.000000000



         30.000000000                              0

         2

         TDN C1 D PROC 1 1 1 4.000000000

         XDN DOWN 0 125000 DEC 1 0



         33.000000000                              1

         1                                         E D POST -1 2 0 1

         XDN DOWN 1 125000 DEC 1 1



         35.500000000                              0

         3

         TDN E D POST -1 2 0 1 1.500000000

         FIN 0

         FIN 1



         END                                       exit

        The two requests may share the local computer tasks even though their
        process tasks run on different remote computers. The two local-to-
        remote transfers are handled one at a time, in remote-computer number
        order; the final D POST combines results delivered by two different
        remote-to-local transfers.




https://codeforces.com/contest/2251/problems#                                                                                                                      7/8
8/15/26, 12:53 AM                                                          Problems - Codeforces

                                                Codeforces (c) Copyright 2010-2026 Mike Mirzayanov
                                                  The only programming contests Web 2.0 platform




https://codeforces.com/contest/2251/problems#                                                        8/8
